<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <title>{{ topic.title() }} Blog</title>
   <link href="https://fonts.googleapis.com/css2?family=Raleway" rel="stylesheet">
   <link rel="stylesheet" href="../static/css/styles.css">
</head>
<body>
<div class="wrapper">
   <div class="top">
       <div class="title"><h1>{{ topic.title() }} Blog</h1></div>
   </div>
   <div class="content">
        {% for post in all_posts %}
           <div class="card">
               <h2><strong>{{ post.title | safe }}</strong></h2>
               <img src="{{ post.thumbnail }}" alt="{{ post.title }}">
               <p class="text">{{ post.subtitle | safe }} </p>
               <a href="{{ url_for('get_article', article_id=post.id) }}">Read</a>
           </div>
       {% endfor %}


   </div>
   <div class="content">
       <div class="card">
           <h2>Another blog post</h2>
           <p class="text">Lorem ipsum dolor sit </p>
           <a href="#">Read</a>
       </div>
   </div>
</div>
</body>
<footer>
   <p>Ripped off with love, from the Guardian.</p>
</footer>
</html>

